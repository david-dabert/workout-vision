/**
 * Telemetry stub — no-op until a real analytics endpoint is configured.
 * Previously pointed to httpbin.org (public echo service = privacy leak).
 */

export function logEvent(_event, _data) {
  // No-op. Replace with real analytics when ready.
}
